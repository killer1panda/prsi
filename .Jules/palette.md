## 2026-08-24 - Accessibility and Inline Errors in frontend.html
**Learning:** Adding ARIA-labels and focus-visible styling is essential for keyboard navigation, and inline errors with `role="alert"` provide a much better user experience than native `alert()` popups.
**Action:** Always prefer inline error messages linked with ARIA roles over native popups, and ensure custom focus styles have sufficient contrast and offset.
