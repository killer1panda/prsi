## Note on GitHub Actions failure
The CI failure `github-advanced-security` with error `Model not supported` is an infrastructure-level error happening within the Copilot Workspace action's runner environment (`CAPIError: 400 The requested model is not supported.`). It is completely independent of the codebase or the changes I introduced to the Next.js frontend or my performance improvements.
